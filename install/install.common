#! /bin/bash

BASE_PACKAGES="${BASE_PACKAGES} wget"
BASE_PACKAGES="${BASE_PACKAGES} git"
BASE_PACKAGES="${BASE_PACKAGES} openssl"
BASE_PACKAGES="${BASE_PACKAGES} net-tools"
BASE_PACKAGES="${BASE_PACKAGES} gdb"
BASE_PACKAGES="${BASE_PACKAGES} cscope"

if [ -f /etc/redhat-release ]; then
    BASE_PACKAGES="${BASE_PACKAGES} emacs"
else
    BASE_PACKAGES="${BASE_PACKAGES} emacs24"
fi
